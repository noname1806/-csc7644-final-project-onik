# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.venv/
venv/
env/
.pytest_cache/

# Local cache (Play Store metadata pulled on demand)
data/cache/

# Editor / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Secrets
.env
.env.local
*.key

# Streamlit
.streamlit/secrets.toml

# Scratch run outputs at repo root (one-off check.py --out destinations)
/lyft_*.json
/whatsapp_*.json

# Fine-tuning artifacts (regenerated by finetune/prepare_data.py and train.py)
finetune/dataset/*.jsonl
finetune/checkpoints/

# HuggingFace / torch caches
.cache/

# Project proposal PDF (local-only, not part of the released artifact)
LLM_PRIVACY_TEXT_POLICY*.pdf
