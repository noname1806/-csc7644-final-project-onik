google-play-scraper>=1.2.7
beautifulsoup4>=4.12.0
requests>=2.31.0
anthropic>=0.39.0
streamlit>=1.30.0
pydantic>=2.5.0

# For the fine-tuning track (optional — only needed for `--backend finetuned`
# or `--backend local-hf`). Install with:
#   pip install -r finetune/requirements.txt
